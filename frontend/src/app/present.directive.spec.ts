import { PresentDirective } from './present.directive';

describe('PresentDirective', () => {
  it('should create an instance', () => {
    const directive = new PresentDirective();
    expect(directive).toBeTruthy();
  });
});
